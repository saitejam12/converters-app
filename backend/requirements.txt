# Runtime.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.9
sqlalchemy>=2.0

# Checks. The platform installs and runs this file before it reports that the
# generated tests pass, so the test dependencies belong here rather than in a
# second file nobody installs. httpx is what fastapi.testclient runs on.
httpx>=0.27
pytest>=8.3
